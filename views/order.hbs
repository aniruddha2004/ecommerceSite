<!DOCTYPE html>
<html lang="en">
<head>
  <title>My Orders - RetroRevive</title>
  <link rel="stylesheet" href="/css/home.css"> <!-- Consistent styling -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/navbar.css">
  <style>
    /* Custom styling for the order cards */
    .order-card {
      background-color: rgba(0, 0, 0, 0.8); /* Black background for cards */
      color: #fbc531; /* Yellow text for titles */
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
      height: 300px;
    }
    .order-card .card-title {
      color: #fbc531; /* Highlighted yellow title */
      font-weight: bold;
    }
    .order-card ul {
      list-style: none;
      padding: 0;
    }
    .order-card ul li {
      color: #fff; /* White text for items */
      margin-bottom: 5px;
    }
    .order-card p {
      color: #fff; /* White text for total and status */
    }
    .order-card p strong {
      color: #fbc531; /* Yellow for labels */
    }
  </style>
</head>
<body>
  {{> navbar }}

  <!-- Jumbotron -->
  <div class="jumbotron text-center">
    <h1 class="display-4">My Orders</h1>
    <p class="lead">Track your past orders and their statuses here.</p>
  </div>

  <!-- Orders Section -->
  <div class="container my-5">
    <h2 class="text-center mb-4">Your Orders</h2>

    {{#if orders.length}}
      <div class="row">
        {{#each orders}}
        <div class="col-md-6 mb-4">
          <div class="order-card">
            <h5 class="card-title">Order #{{this._id}}</h5>
            <ul>
              {{#each this.items}}
              <li>{{this.product.name}} (x{{this.quantity}})</li>
              {{/each}}
            </ul>
            <p><strong>Total:</strong> ₹{{this.total}}</p>
            <p><strong>Status:</strong> {{this.status}}</p>
          </div>
        </div>
        {{/each}}
      </div>
    {{else}}
      <div class="text-center">
        <p class="lead">You have no orders yet. Start shopping to create your first order!</p>
        <a href="/products" class="btn btn-primary btn-lg">Explore Products</a>
      </div>
    {{/if}}
  </div>

  <!-- Footer -->
  <footer class="footer">
    <p>&copy; 2024 Vintage Finds & Upcycling. All rights reserved.</p>
  </footer>

  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
