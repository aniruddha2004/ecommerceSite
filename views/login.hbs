<!DOCTYPE html>
<html lang="en">
<head>
  <title>Login - RetroRevive</title>
  <link rel="stylesheet" href="/css/home.css"> <!-- Consistent styling -->
  <link rel="stylesheet" href="/css/navbar.css">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <style>
    /* Login form container styling */
    .form-container {
      background-color: rgba(0, 0, 0, 0.8); /* Dark background for the form */
      color: #fbc531; /* Yellow text for labels and headings */
      border-radius: 10px;
      padding: 30px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
      max-width: 500px;
      margin: auto;
      margin-top: 85px;
    }
    .form-container h2 {
      color: #fbc531; /* Yellow for heading */
      text-align: center;
      margin-bottom: 20px;
    }
    .form-container .form-group label {
      color: #fff; /* White text for labels */
      font-weight: bold;
    }
    .form-container .form-control {
      background-color: #333; /* Dark background for input fields */
      color: #fff; /* White text for inputs */
      border: 1px solid #fbc531; /* Yellow border */
      border-radius: 5px;
    }
    .form-container .form-control:focus {
      background-color: #444;
      border-color: #fbc531; /* Yellow border on focus */
      box-shadow: 0 0 5px rgba(251, 197, 49, 0.7); /* Glow effect */
    }
    .form-container button {
      background-color: #fbc531; /* Yellow button */
      color: #000;
      border: none;
      font-weight: bold;
      width: 100%;
    }
    .form-container button:hover {
      background-color: #e1a321;
    }
    .form-container p {
      color: #fff;
      text-align: center;
      margin-top: 15px;
    }
    .form-container p a {
      color: #fbc531; /* Yellow link */
      text-decoration: none;
    }
    .form-container p a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <!-- Login Form -->
  <div class="container my-5">
    <div class="form-container">
      <h2>Login</h2>
      <form action="/login" method="POST">
        <div class="form-group">
          <label for="username">Username</label>
          <input type="text" class="form-control" id="username" name="username" required>
        </div>
        <div class="form-group">
          <label for="password">Password</label>
          <input type="password" class="form-control" id="password" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary btn-lg mt-3">Login</button>
      </form>
      <p class="mt-3">Don't have an account? <a href="/register">Register here</a></p>
    </div>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
